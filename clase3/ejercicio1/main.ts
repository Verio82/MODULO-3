// importe mathUtils y utilice ambas funciones para mostrar el resultado en la consola.

import { add, subtract } from './mathUtils';

const num1 = 10;
const num2 = 5;

console.log(`La suma de ${num1} y ${num2} es: ${add(num1, num2)}`);
console.log(`La resta de ${num1} y ${num2} es: ${subtract(num1, num2)}`);
    