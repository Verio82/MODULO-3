//utiliza el namespace Geometry para llamar a las funciones y muestra los resultados en la consola. 

import { Geometry } from './geometry';



console.log(`Área del círculo= `, Geometry.areaOfCircle(5));
console.log(`Área del cuadrado= `, Geometry.areaOfSquare(4));


